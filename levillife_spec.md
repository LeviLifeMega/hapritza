# איפיון טכני - מערכת תפעול סדנה קבוצתית
## Levillife - הפריצה הגדולה

---

## 1. מטרת המערכת

מערכת פנימית לניהול תפעולי מלא של סדנת "הפריצה הגדולה" - תהליך קבוצתי של 10 שיעורים פרונטליים.

המערכת מיועדת לצוות הביצוע (מור, עדי, יסמין) ולאייל, ומכסה את כל מה שצריך לעשות לפני כל שיעור, במהלכו ואחריו.

**עקרון מרכזי:** התוכן (טקסטים, קישורים, צ'קליסטים) חי בקבצי JSON נפרדים מהממשק. עריכת תוכן לא דורשת נגיעה בקוד.

---

## 2. ארכיטקטורה טכנית

```
/
├── index.html          ← ממשק ראשי
├── style.css           ← עיצוב (RTL מלא, עברית)
├── app.js              ← לוגיקת ממשק
├── data/
│   ├── general.json    ← מידע כללי, הקמה, דגשים למנטורים
│   ├── lesson_01.json  ← כל הנתונים של שיעור 1
│   ├── lesson_02.json  ← כל הנתונים של שיעור 2
│   └── ...lesson_10.json
└── assets/
    └── images/         ← תמונות לכל שיעור
```

**שפת פיתוח:** HTML + CSS + Vanilla JavaScript (ללא frameworks)
**RTL:** `dir="rtl"` על כל המסמך, גופן Heebo מ-Google Fonts
**אחסון state:** localStorage לשמירת מצב צ'קליסטים
**ניהול קוד:** GitHub (עריכה מכל מחשב)

---

## 3. מבנה הניווט הראשי

### סרגל צד שמאלי (sidebar) קבוע:

```
LEVILLIFE
מערכת תפעול - הפריצה הגדולה
─────────────────────────
▸ מידע כללי
▸ הקמת הסדנה
▸ דגשים למנטורים
─────────────────────────
שיעורים
▸ שיעור 1 - [שם השיעור]
▸ שיעור 2 - [שם השיעור]
▸ שיעור 3 - [שם השיעור]
▸ שיעור 4 - [שם השיעור]
▸ שיעור 5 - [שם השיעור]
▸ שיעור 6 - [שם השיעור]
▸ שיעור 7 - [שם השיעור]
▸ שיעור 8 - [שם השיעור]
▸ שיעור 9 - [שם השיעור]
▸ שיעור 10 - מפגש מסכם
─────────────────────────
▸ מה קורה בין המפגשים?
```

---

## 4. מבנה דף שיעור - טאבים

כל שיעור מורכב מ-5 טאבים:

```
[ לפני השיעור ] [ הגעה ותפעול ] [ במהלך השיעור ] [ אחרי השיעור ] [ קישורים וקבצים ]
```

---

### טאב 1: לפני השיעור

**מטרה:** כל מה שצריך להכין/לבדוק/לרכוש לפני הגעה לשיעור.

**מבנה:**

#### קטע א - בדיקות מראש
רשימת צ'קליסט עם checkboxes.
כל פריט יכול להיות:
- משימה רגילה (checkbox)
- משימה עם הערת ניואנס (checkbox + אייקון מידע שנפתח בלחיצה)
- משימה עם קישור (checkpoint + link)

דוגמה מהמסמכים:
```json
{
  "section": "בדיקות מראש",
  "items": [
    { "text": "לבדוק שיש דיו במדפסת", "type": "checkbox" },
    { "text": "לבדוק שיש דפים למדפסת", "type": "checkbox" },
    { 
      "text": "לבדוק שיש מדבקות קטנות עם הלוגו",
      "type": "checkbox",
      "note": "מדבקות עם הלוגו של אייל אברהם לוי"
    }
  ]
}
```

#### קטע ב - מה לרכוש / להכין
רשימת צ'קליסט נפרדת לרכישות והכנות פיזיות.
כולל כמויות ספציפיות שמשתנות לפי מספר התלמידים.

**תכונה:** שדה עריכה למספר התלמידים בכל שיעור → המערכת מחשבת כמויות אוטומטית.
```
מספר תלמידים: [ 25 ]
↓
"להדפיס 2 מדבקות × 25 = 50 מדבקות"
```

#### קטע ג - דפי עבודה להדפסה
רשימת הקבצים שצריך להדפיס לשיעור הזה, עם:
- שם הדף
- כמות (מחושבת לפי מספר תלמידים)
- קישור להורדת PDF מ-Google Drive
- כפתור "אישור הדפסה נשלח לאייל" (toggle)

---

### טאב 2: הגעה ותפעול טכני

**מטרה:** הכנת המרחב הפיזי והציוד הטכני לפני הגעת התלמידים.

**מבנה:**

#### קטע א - סידור המתחם
צ'קליסט לסידור פיזי של החלל.
```json
{
  "items": [
    "לסדר את המתחם לפי הרשימה בקונקטים",
    "לסדר ישיבה לפי מספר התלמידים",
    "לחתוך מגשי כיבוד פירות וירקות",
    "לסדר צלוחיות פיצוחים"
  ]
}
```

#### קטע ב - ציוד טכני
צ'קליסט עם הדגשות טכניות:
- בדיקת מצלמה
- בדיקת מיקרופון
- חיבור זום + קישור זום לשיעור (שדה להכנסת לינק)
- בדיקת מצגת (קישור ל-Google Drive)
- הגדרות סאונד - מיקסר
- בדיקת תאורה
- הגדרת טמפרטורה ואוורור
- בדיקת הקלטה (הפעלה לפני תחילת שיעור, עצירה אחרי הפסקה)

#### קטע ג - מוזיקה
רשימת פלייליסטים לפי סוג:
- מוזיקת כתיבה
- מוזיקה חיובית / אנרגטית
- מוזיקה להקפצה
עם קישור לכל פלייליסט.

---

### טאב 3: במהלך השיעור

**מטרה:** מדריך תפעולי לאייל ולמנטורים במהלך השיעור עצמו.

**מבנה:**

#### קטע א - מבנה השיעור
רשימת שלבי השיעור לפי סדר, כל שלב עם:
- שם השלב
- זמן משוער
- תיאור קצר מה עושים
- ניואנסים (expandable toggle) - מה לעשות בתרחישים שונים

#### קטע ב - דגשים ספציפיים לשיעור הזה
הדגשים החשובים שרלוונטיים לשיעור הספציפי הזה (שונה משיעור לשיעור).

#### קטע ג - נקודות חשובות לפתיחה
- פתיחה עם תובנות, שיתופים והצלחות מהשיעור הקודם
- נהלי שאלות ותשובות

---

### טאב 4: אחרי השיעור

**מטרה:** כל מה שצריך לעשות אחרי שהתלמידים יוצאים.

**מבנה:**

#### קטע א - הודעות WhatsApp לשליחה
לכל שיעור יש הודעות ספציפיות לשלוח.
כל הודעה מוצגת בתיבה עם:
- שם ההודעה (מה מטרתה)
- מיהו הנמען (קבוצת ווצאפ ראשית / קבוצת מנטורים / הודעה אישית)
- הטקסט המלא של ההודעה - עם כפתור **"העתק"** בלחיצה אחת
- קישורים/תמונות שמצורפות להודעה

דוגמה:
```json
{
  "messages": [
    {
      "name": "הודעת סיכום שיעור 1",
      "recipient": "קבוצת ווצאפ - הפריצה הגדולה",
      "text": "...[טקסט ההודעה]...",
      "attachments": ["קישור לשיעור באתר", "תמונה מהשיעור"]
    }
  ]
}
```

#### קטע ב - עריכת הקלטה
צ'קליסט לתהליך עריכת ההקלטה:
- הורדת קובץ הקלטה
- עריכה (חיתוך הפסקה, תחילה וסוף)
- העלאה לפלטפורמה
- שליחת קישור לאייל לאישור
- פרסום

שדה להכנסת קישור לקלטה המוכנה לאחר עריכה.

#### קטע ג - פתיחת שיעור באתר החברים
צ'קליסט עם שלבים לפתיחת השיעור הבא באתר:
- כניסה לממשק ניהול האתר
- פתיחת נעילה לשיעור הבא
- בדיקה שהשיעור נפתח כראוי
- שליחת הודעת ווצאפ על פתיחת השיעור

שדה: קישור לשיעור באתר לאחר פתיחה.

#### קטע ד - שליחת תמונות לווצאפ
- רשימת התמונות שצריך לשלוח לאחר כל שיעור
- לאיזה קבוצה/אנשים לשלוח
- צ'קליסט של אישורים

#### קטע ה - סגירה כללית
צ'קליסט סיכום:
- ניקוי ותיאום המתחם
- דיווח לאייל על תקלות / הצלחות בולטות
- עדכון גיליון הנוכחות
- הכנה ראשונית לשיעור הבא

---

### טאב 5: קישורים וקבצים

**מטרה:** כל הקישורים הרלוונטיים לשיעור הספציפי הזה.

**מבנה:**

```
מצגות
├── קישור למצגת Google Drive (לחצן "פתח מצגת")
└── סטטוס: [ מאושרת ✓ / לא מאושרת - לוודא עם אייל ]

דפי עבודה
├── [שם דף עבודה 1] - [כמות להדפסה] - כפתור הורדה
└── [שם דף עבודה 2] - [כמות להדפסה] - כפתור הורדה

תמונות לשיעור
└── גלריה של התמונות לשלוח בווצאפ

הקלטה
└── שדה לקישור ההקלטה הגמורה
```

---

## 5. מבנה דפי ה-JSON לכל שיעור

```json
{
  "lesson_number": 1,
  "title": "איך להוציא את המקסימום מהתהליך",
  "subtitle": "שיעור פתיחה",

  "before": {
    "preparations": [
      {
        "id": "prep_01",
        "text": "לבדוק שיש דיו במדפסת",
        "category": "ציוד",
        "note": null
      }
    ],
    "purchases": [
      {
        "id": "buy_01",
        "text": "בלונים בצבעים: סגול, תכלת, ורוד, זהב, אפור",
        "quantity_formula": "40",
        "supplier": "חנות יאלי בשכונה",
        "phone": "03-6731983",
        "note": "עושים לנו 4 שקל לבלון - לוודא מחיר"
      }
    ],
    "print_materials": [
      {
        "id": "print_01",
        "name": "גלגל החיים",
        "quantity_formula": "students_count",
        "drive_link": "https://drive.google.com/...",
        "requires_approval": true
      }
    ]
  },

  "setup": {
    "venue_checklist": [...],
    "technical_checklist": [...],
    "zoom_link": "",
    "presentation_drive_link": "https://drive.google.com/...",
    "music_playlists": [...]
  },

  "during": {
    "stages": [
      {
        "id": "stage_01",
        "title": "פתיחה",
        "duration_minutes": 15,
        "description": "...",
        "nuances": [
          {
            "trigger": "אם יש אנרגיה נמוכה בחדר",
            "response": "..."
          }
        ]
      }
    ],
    "special_notes": [...]
  },

  "after": {
    "whatsapp_messages": [
      {
        "id": "msg_01",
        "name": "הודעת סיכום שיעור",
        "recipient": "קבוצת ווצאפ ראשית",
        "text": "...",
        "attachments": []
      }
    ],
    "recording_checklist": [...],
    "website_unlock_checklist": [...],
    "photos_to_send": [...],
    "closing_checklist": [...]
  },

  "files": {
    "presentation": {
      "drive_link": "",
      "approved": false
    },
    "work_sheets": [...],
    "recording_link": "",
    "photos": []
  }
}
```

---

## 6. הדפים הכלליים (לא ספציפיים לשיעור)

### דף: מידע כללי
- פרטי הסדנה (שם, תיאור, מספר שיעורים)
- מספר התלמידים הנוכחי (שדה עריכה גלובלי שמשפיע על כל הכמויות)
- לוח זמנים של שיעורים (תאריכים)
- פרטי מיקום

### דף: הקמת הסדנה
צ'קליסט חד-פעמי להקמת הסדנה (עשה פעם אחת):
- פתיחת קבוצת פייסבוק (שם, תיאור, תמונת כריכה, הגדרות פרטיות)
- פתיחת קבוצת ווצאפ (שם, תמונת קבוצה, הוספת משתתפים)
- הגדרת אתר החברים (קישורים, לוח גישה)
- הגדרות זום (פרמטרים קבועים)
- הגדרת תיקיית Google Drive לסדנה
- שליחת הודעת ברוכים הבאים ראשונית

### דף: דגשים למנטורים
מדריך לכל מנטור שמעביר שיעור:
1. שליטה על המצגן (טמפרטורה, אוורור)
2. עבודה עם התאורה
3. עבודה עם המדמה + מיקרופון יד
4. מוזיקה - רשימות ואיך לעבוד עם המיקסר
5. הקלטה - הפעלה, עצירה בהפסקה, חידוש
6. זום - חיבור, מיקרופון, מצלמה
7. עבודה עם המצלמה
8. כפתורי האלגנו
9. כללי לבוש

### דף: מה קורה בין המפגשים?
מדריך לתהליכים שמתרחשים בין שיעור לשיעור:
- תזכורות לתלמידים
- מעקב אחר ביצוע מטלות
- תמיכה בקבוצת ווצאפ
- הכנות לשיעור הבא

---

## 7. תכונות ממשק חובה

### שמירת מצב צ'קליסטים
- כל checkbox נשמר ב-localStorage
- מפתח: `lesson_{number}_{section}_{item_id}`
- אינדיקטור התקדמות לכל טאב: "3/7 הושלמו"
- אינדיקטור כללי לכל שיעור בסרגל הצד

### כפתור "העתק הודעה"
לכל הודעת ווצאפ - כפתור שמעתיק את הטקסט ל-clipboard עם אנימציית אישור ("הועתק ✓").

### חישוב כמויות דינמי
שדה גלובלי "מספר תלמידים" בראש כל שיעור.
כל פריט עם `quantity_formula: "students_count"` מציג: `[כמות] × [מספר תלמידים] = [סה"כ]`

### קישורים לגוגל דרייב
כפתורי "פתח" שפותחים קישורים בטאב חדש.
סטטוס אישור מצגת (approved: true/false) מוצג כ-badge.

### חיפוש
שדה חיפוש בראש הדף שמחפש בכל השיעורים ומחזיר תוצאות עם קישור לשיעור ולקטע הרלוונטי.

### מצב הדפסה
כפתור "הדפס שיעור זה" שיוצר גרסה נקייה להדפסה של כל הצ'קליסטים של שיעור ספציפי.

---

## 8. עיצוב ונגישות

### שפה וכיוון
- **שפה:** עברית בלבד
- **כיוון:** RTL מלא (`dir="rtl"` על `<html>`)
- **גופן:** Heebo (Google Fonts) - תמיכה מלאה בעברית
- **נייד:** responsive מלא - עובד על נייד ללא גלילה אופקית

---

## 8א. זהות ויזואלית ומותג

### הלוגואים שמגדירים את הסגנון
שני לוגואים נשלחו כהפניה:
1. **לוילייף תוצאות על בע"מ** - טקסט סגול כהה, לב צבעוני עם פרפרים ואלמנטים חיים
2. **הגשמה עצמית Express** - רקע שחור כהה, פיצוץ צבעוני של טיפות מעל הר געש

### פלטת צבעים

```css
:root {
  /* רקעים - כהה כמו לוגו הגשמה עצמית */
  --bg:         #08090D;   /* רקע ראשי - שחור עמוק */
  --bg2:        #0F1018;   /* רקע משני */
  --bg3:        #15161F;   /* כרטיסים ופאנלים */

  /* גבולות */
  --border:     rgba(255,255,255,0.07);
  --border2:    rgba(255,255,255,0.12);

  /* טקסט */
  --text1:      #F2EEFA;   /* טקסט ראשי - לבן חם */
  --text2:      #9490AA;   /* טקסט משני */
  --text3:      #5A5670;   /* טקסט שלישוני */

  /* צבעי מותג - מתוך פיצוץ הצבעים בלוגו */
  --purple:     #7B4FD4;   /* סגול - צבע מוביל */
  --pink:       #E83B8C;   /* ורוד/מגנטה */
  --blue:       #29A8E0;   /* כחול */
  --teal:       #2DC4B0;   /* טורקיז */
  --green:      #52D44A;   /* ירוק */
}
```

### שימוש בצבעים בממשק

כל שלב/שיעור מקבל צבע אחד מהפלטה הצבעונית:
- שלב 1 / שיעורים 1,6  → `--purple`
- שלב 2 / שיעורים 2,7  → `--blue`
- שלב 3 / שיעורים 3,8  → `--pink`
- שלב 4 / שיעורים 4,9  → `--teal`
- שלב 5 / שיעורים 5,10 → `--green`

כרטיס שלב פעיל מקבל:
```css
background: rgba([rgb של הצבע], 0.10);
border: 1.5px solid rgba([rgb של הצבע], 0.40);
/* הטקסט והאייקון בצבע המלא */
```

### לוגו בממשק
- לוגו לוילייף מוצג בפינה הימנית עליונה של ה-sidebar
- מתחתיו: שם המערכת "הפריצה הגדולה - מערכת תפעול"
- הלוגו נטען מ: `assets/images/logo-levillife.png`

### אלמנט חתימה
נקודת גרדיאנט קטנה ליד ה-brand label:
```css
.brand-dot {
  width: 10px; height: 10px; border-radius: 50%;
  background: linear-gradient(135deg, #7B4FD4, #E83B8C, #29A8E0);
}
```
מייצגת את "פיצוץ הצבעים" מהלוגו בצורה מינימליסטית.

---

## 9. מה לא כלול במערכת זו

הדברים הבאים נמצאים מחוץ לממשק הזה:
- ממשק לתלמידים (זה פנימי לצוות בלבד)
- מערכת תשלומים
- ניהול לידים
- CRM

---

## 10. פקודות לקלוד קוד

```bash
# התקנה ראשונית
mkdir levillife-ops
cd levillife-ops
git init

# מבנה תיקיות
mkdir data assets/images

# הפעלת שרת מקומי לבדיקה
python3 -m http.server 8080
# או: npx serve .
```

**סדר בנייה מומלץ:**
1. `index.html` עם layout בסיסי + sidebar
2. `style.css` עם RTL מלא + עיצוב
3. קובץ `data/lesson_01.json` לדוגמה עם נתוני שיעור 1
4. `app.js` עם טעינת JSON והצגת שיעור
5. לוגיקת צ'קליסטים + localStorage
6. לוגיקת כפתור "העתק הודעה"
7. שאר 9 קבצי ה-JSON
8. דפים כלליים (מנטורים, הקמה, בין מפגשים)

---

*מסמך איפיון v1.0 - Levillife Workshop Operations System*
*נוצר: יוני 2026*
